<%= render partial: 'list_show', locals: { list: @list } %>

<%= button_to "Add Item", new_list_item_path(@list), method: :get %>

<%= button_to "Add User to List", list_update_members_path(@list), method: :get %>

<%= button_to "Edit List Properties", edit_list_path(@list), method: :get %>

<%= button_to "Back to Home", user_path(current_user), method: :get %>